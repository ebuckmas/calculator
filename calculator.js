//create basic math functions

const add = (x,y) => x + y;

const subtract = (x,y) => x - y;

const multiply = (x,y) => x * y;

const divide = (x,y) => x / y;

//create operator function that takes in operation to perform plus two numbers

const operate = (operator, x, y) => operator(x,y);
